using UnityEngine;
using System.Collections;

public class NodeController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//rigidbody.velocity = new Vector3(-0.1f,0,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
